# Leave empty for local dev (dashboard reads frontend/public/data/incidents_latest.json,
# written by running python fetch_incidents.py yourself).
# Set this in Vercel's project settings once your backend is live on Render:
VITE_API_URL=https://utcp-soc-dashboard.onrender.com